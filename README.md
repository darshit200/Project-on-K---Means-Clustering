# Project-on-K---Means-Clustering
This project demonstrates K-Means Clustering using Python, where data points are grouped into clusters based on distance from centroids. The project also visualizes the final clusters and centroids using Matplotlib.
